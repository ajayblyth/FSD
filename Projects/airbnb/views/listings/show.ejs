<% layout("/layout/boilerplate") %>
<script>
    const mapToken = "<%= process.env.MAP_TOKEN %>";
const listingCoordinates = <%= JSON.stringify(listing.geometry.coordinates) %>;
</script>
    <div class="row">
        <div class="col-8 offset-2">
            <h3>Listing Details :< /h3>
        </div>
        <div class="card col-6 offset-2 show-card">
            <img src="<%= listing.image %>" class="card-img-top show-img" alt="listing_img">
            <div class="card-body">
                <p class="card-text">
                    <b>

                        <%= listing.title %>
                    </b><br>
                    <strong>Owner :< /strong>
                            <%= listing.owner.username %><br>
                                <%= listing.description %><br>
                                    &#8377; <%= listing.price.toLocaleString() %><br>
                                        <%= listing. location %><br>
                                            <%= listing.country %>
                </p>
            </div>
        </div>

<% if (currentUser && currentUser._id.equals(listing.owner._id)) { %>
        <div.class="btns .col-8.offset-2.ps-4">
< a.href="/listings/<%=listing ._ id %>/edit" class="btn-btn-dark">.edit .< /a>
< a.href="/listings/<%= listing ._ id %> ?_ method=DELETE".class="btn btn-danger">delete .< /a>
< /div>
<% } %>

<div class="col-8 offset-2 mt-5">
<h4>Leave a Review </h4>
<form action="/listings/<%= listing. id %>/reviews" method="POST" class="need-validation" novalidate>

    <div class="mb-3" >
        <label for="review" class="form-label">Review :< /label>
        <textarea name="review[body]" id="review" cols="30" rows="5" class="form-control" required></textarea>
        <div class="invalid-feedback">
            Please provide a review.
        </div>
    </div>
       

    <% if (listing.reviews.length > 0) { %>
<div class="col-8 offset-2 mt-5">
<h4>All Reviews</h4>
<div class="row">
<% for(let review of listing.reviews) { %>
<div class="col-sm-6 mb-3 mb-3">
<div class="card">
<div class="card-body">
<h5 class="card-title"><%= review.author.username %></h5>
<p class="card-text"><%= review.comment %></p>
<p class="starability-result" data-rating="<%= review.rating %>">
Rated: <%= review.rating %> stars
</p>
<form class="mb-3" method="POST" action="/listings/<%= listing ._ id %>/reviews/<%= review. id %>? meth
button type="submit" class="btn btn-danger">Delete</button>
</form>
</div>

update from 8:05// 18th may 