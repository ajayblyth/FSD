Deploy React on EC2
Build with Node (as ec2-user), serve with Apache (sudo)

t3.small, AL2023, open 22 + 80

1

Launch

2

Connect

ssh in as ec2-user

3

Install (sudo)

Apache + git via dnf

4

Node (no sudo)

nvm -> node/npm, for ec2-user

5

Build

git clone -> npm install -> npm run build -> dist

6

Serve (sudo)

copy dist/* to /var/www/html -> restart Apache

We never become root - sudo only for install, Apache, and /var/www/html. Node stays with ec2-user.

==================
start from 5:57
start Apache
sudo systemcl start httpd

for status
sudo systemcl start httpd

to keep it running even on reboot
sudo systemcl enable httpd
-------------------------------------------
note: search aws linux image node install ec2, you will get documentation which give steps....click on docs.aws.amazon.com link, thats official documentation
install node

nvm: node version package manager, manages multiple node versions

-------------------------------------
install nvm from docs.aws.amazon.com
copy and paste in cli

now install node version with nvm, lts is stable version on nodejs website
nvm install -lts 

check  node --version
npm --version


now pwd in cli
cd/home/ec2-user/
git clone repository link

list
ls

select folder inside repository..
cd React/app

npm install to install node modules

ls

npm run build

will create one dist folder
all files will be converted to single index.html

cd..

remove if some files are there
sudo rm -rf /var/www/html/*


copy dist file here, from current location to destination

sudo cp -r dist/* /var/www/html/

restart server
sudo systemctl restart httpd
now open public ip in browser




this is how we deploy react app... now go to aws , actions , stop instance, but our hard didk will still be there and billing is going on, so delete that too

----
for node js do it differently as mongodb is also related