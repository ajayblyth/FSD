=====================================================================
NPM (NODE PACKAGE MANAGER) - NODE.JS (CLEAN + STRUCTURED)
=====================================================================

DEFINITION
---------------------------------------------------------------------
• NPM → Default package manager for Node.js
• Used to install, manage, share packages (npmjs.com)

---------------------------------------------------------------------
KEY COMPONENTS
---------------------------------------------------------------------

1. PACKAGE
   • Reusable code (library/module)
   • Example → express, axios

---------------------------------------------------------------------

2. package.json (MAIN FILE)
   • Stores project info, 
   dependencies, scripts

DEPENDENCIES
---------------------------------------------------------------------
• dependencies → required to run app (production)
• examples → express, axios
• install → npm install <pkg>
• uninstall → npm uninstall <pkg>   (e.g., npm uninstall express)

DEVDEPENDENCIES
---------------------------------------------------------------------
• devDependencies → Only for development
• Example → nodemon, jest (JavaScript testing framework )
• Install → npm install <pkg> --save-dev

nodemon- • Tool that automatically restarts Node.js server when files change  
• Used in development (not production)

1) GLOBAL INSTALL (OLD / SIMPLE WAY)
=====================================================================

npm install -g nodemon

RUN:
nodemon app.js

✔ Works from anywhere in system  
❌ Not project-specific (not recommended in modern projects)


=====================================================================
2) LOCAL INSTALL (RECOMMENDED)
=====================================================================

npm install --save-dev nodemon

RUN:
npx nodemon app.js

✔ Installed inside project  
✔ Works per project  
✔ Best practice

Note: npx (Node Package Execute)
npx is a Node tool that runs npm packages directly without installing
them globally, by temporarily fetching and executing them.

HOW IT WORKS
=====================================================================
1) Checks if package is installed locally  
2) If not → downloads temporarily  
3) Runs it  
4) Deletes temporary copy (if not installed)

DIFFERENCE
---------------------------------------------------------------------
• dependencies → runtime
• devDependencies → development
• Production skips devDependencies

---------------------------------------------------------------------

3. SCRIPTS
---------------------------------------------------------------------
• Custom commands inside package.json
• Format → "key": "command"

{
  "name": "my-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "hello": "echo Hello Ajay"
  }
}


=====================================================================
HOW TO RUN
=====================================================================

npm start
→ runs: node app.js

npm run dev
→ runs: nodemon app.js

npm run hello
→ output: Hello Ajay

Note:
• "start" is special → can run without "run"  
• Others need → npm run <name>

---------------------------------------------------------------------

4. node_modules
---------------------------------------------------------------------
• Folder storing all installed packages
• Auto-created by npm

---------------------------------------------------------------------

5. package-lock.json
---------------------------------------------------------------------
• Auto-generated file
• Stores exact versions (no changes)
• Ensures same install everywhere

=====================================================================
package.json vs node_modules (DEPENDENCIES)
=====================================================================

📦 package.json
------------------------------------------------
• Your project’s dependency list (blueprint)
• Only shows WHAT you need

Example:
"dependencies": {
  "express": "^4.18.0"
}

👉 It does NOT contain actual code

📦 node_modules
------------------------------------------------
• Actual downloaded packages (real code)
• Contains express + ALL its internal dependencies

👉 This is WHERE code actually lives


=====================================================================
HOW THEY ARE CONNECTED
=====================================================================

package.json
     ↓
npm install
     ↓
node_modules gets created

👉 npm reads package.json and installs everything into node_modules


=====================================================================
IMPORTANT CONCEPT (INTERVIEW)
=====================================================================

✔ package.json → direct dependencies (your choice)
✔ node_modules → full dependency tree (your + their dependencies)

=====================================================================
KEY POINT
=====================================================================

👉 You never manually manage node_modules  
👉 npm builds it automatically using package.json

------------------------------------------------------------------
NPM COMMANDS (EXPLAINED)
------------------------------------------------------------

1. npm init
• First command to run in any Node project ✔
• Creates package.json by asking questions (name, version, etc.)
• Sets up project configuration

------------------------------------------------------------

2. npm init -y
• Same as npm init but skips all questions
• Automatically creates package.json with default values
• Faster way to start project

------------------------------------------------------------

3. npm install <pkg>
• Installs package locally (inside node_modules)
• Adds it to dependencies in package.json
• Used when package is needed only for this project

------------------------------------------------------------

4. npm install -g <pkg>
• Installs package globally (usable anywhere in system)
• Not added to project’s package.json
• Used for tools like nodemon, typescript

------------------------------------------------------------

5. npm uninstall <pkg>
• Removes package from node_modules
• Also removes from package.json dependencies
• Keeps project clean from unused packages

------------------------------------------------------------

6. npm install pkg@version
• Installs a specific version of a package
• Useful to avoid breaking changes from latest version
• Example: npm install express@4.18.2

------------------------------------------------------------

SUMMARY
------------------------------------------------------------
Start → npm init ✔
Then install/remove/manage packages using npm commands

---------------------------------------------------------------------
LOCAL vs GLOBAL (IMPORTANT)
---------------------------------------------------------------------
• Default → local install (node_modules)
• Works only inside that project
• Running app.js outside → module not found
• Global → npm install -g <pkg>
• Can run anywhere (e.g., nodemon)

---------------------------------------------------------------------
WORKFLOW
---------------------------------------------------------------------
1. npm init
2. npm install <pkg>
3. Use in code
4. Run using scripts

---------------------------------------------------------------------
PACKAGE.JSON vs PACKAGE-LOCK.JSON
---------------------------------------------------------------------
• package.json → what to install
• package-lock.json → exact installed version

Note:
✔ node → runs JavaScript outside browser  
✔ npm → installs packages (libraries)  
✔ npx → runs packages without installing globally  
=====================================================================
NPM LINK (CONNECT LOCAL PACKAGE TO APP)
=====================================================================
Create a local package (library) and connect it to another app
using npm link (live connection)
=====================================================================
FOLDER STRUCTURE
=====================================================================

📁 my-utils (LOCAL PACKAGE / LIBRARY)
------------------------------------------------
my-utils/
   ├── index.js
   ├── package.json


📁 my-app (MAIN PROJECT)
------------------------------------------------
my-app/
   ├── app.js
   ├── package.json


=====================================================================
STEP 1: CREATE LOCAL PACKAGE (my-utils)
=====================================================================

📄 index.js
------------------------------------------------
function add(a, b) {
    return a + b;
}

module.exports = { add };


📄 package.json
------------------------------------------------
{
  "name": "my-utils",
  "version": "1.0.0",
  "main": "index.js"
}


=====================================================================
STEP 2: LINK LOCAL PACKAGE (GLOBAL LINK)
=====================================================================

Open terminal inside:

my-utils/

Run:
------------------------------------------------
npm link

👉 This creates a GLOBAL link for my-utils


=====================================================================
STEP 3: CONNECT TO APP
=====================================================================

Go to:

my-app/

Run:
------------------------------------------------
npm link my-utils

👉 Now my-app is connected to my-utils


=====================================================================
STEP 4: USE IN APP
=====================================================================

📄 app.js
------------------------------------------------
const utils = require("my-utils");

console.log(utils.add(2, 3));


OUTPUT:
------------------------------------------------
5


=====================================================================
HOW IT WORKS
=====================================================================

my-utils   → npm link (creates global reference)
my-app     → npm link my-utils (connects to it)

👉 No copying of code
👉 Direct live connection

=====================================================================
TEST LIVE CHANGE
=====================================================================

If you change:

add function in my-utils

👉 my-app automatically uses updated code

-- npm link connects a local package to another project by creating a
symbolic link, allowing both to share code without publishing to npm.
import utils from "my-utils";
=====================================================================
🔥 HOW IT ACTUALLY WORKS
=====================================================================

/my-utils   → npm link (creates global link)
/my-app     → npm link my-utils (connects to global link)

👉 So /my-app is NOT copying code
👉 It is directly using /my-utils (live connection)


=====================================================================
⚠️ IMPORTANT RULE
=====================================================================

For this to work:

✔ package.json inside my-utils MUST have:

{
  "name": "my-utils"
}

👉 Name must match exactly "npm link my-utils"


=====================================================================
✔ SIMPLE ANALOGY
=====================================================================

my-utils = library box 📦
npm link = making it globally available 🔗
my-app   = plugging into that box


=====================================================================
✔ ONE LINE
=====================================================================

👉 First run npm link inside library folder, then run npm link <package-name> inside app to connect both projects.
=====================================================================
=====================================================================
WHY USE NPM
=====================================================================

• Huge ecosystem
• Easy dependency management
• Saves development time
=====================================================================
REQUIRE vs IMPORT (NODE.js)
------------------------------------------------------------

BASICS
------------------------------------------------------------
1. require → CommonJS (older)
2. import  → ES Modules (modern)

SYNTAX
------------------------------------------------------------
3. require → const x = require('module')
4. import  → import x from 'module'

USAGE
------------------------------------------------------------
5. require → Default in older Node.js
6. import  → Needs "type":"module" in package.json

BEHAVIOUR
------------------------------------------------------------
7. require → Synchronous (loads immediately, blocks)
8. import  → Static import (resolved before execution)

IMPORT vs IMPORT()
------------------------------------------------------------
9. import   → Static, top-level only
10.import() → Dynamic, can be used anywhere

11.import   → Loads module at start
12.import() → Loads on demand (lazy loading)

13.import   → Cannot use await
14.import() → Async, returns Promise (can use await)

SELECTIVE IMPORT
------------------------------------------------------------
15.require → Imports full module (module.exports)
16.import  → Selective → import {a, b} from 'module'
17.Example → import {add, sub} from "./math.js"

IMPORT ALL (NAMESPACE IMPORT)
------------------------------------------------------------
18.import * as math from "./math.js"
19.Access → math.add(), math.sub()

NOTE
------------------------------------------------------------
20.require → No config needed
21.import  → Modern, cleaner & optimized

NOTE
------------------------------------------------------------
• Use either CommonJS OR ES Modules in a project ✔
• Mixing both can cause errors ❌

------------------------------------------------------------

1. COMMONJS (OLD STYLE)
------------------------------------------------------------
• Uses require and module.exports

Example:
const greet = require('./file');
module.exports = greet;

------------------------------------------------------------

2. ES MODULES (NEW STYLE)
------------------------------------------------------------
• Uses import and export

Example:
import greet from './file.js';
export default greet;

------------------------------------------------------------

IMPORTANT
------------------------------------------------------------
• To use ES Modules → add in package.json:
  "type": "module"

• File extension (.js) required in import

---------------------------
LIBRARY
------------------------------------------------------------
• Collection of reusable functions/code
• You control when and how to use it
• Used for specific tasks (flexible)
• Example → axios, lodash

FRAMEWORK
------------------------------------------------------------
• Complete structure for building apps
• Controls flow (calls your code)
• Follows rules & predefined architecture
• Example → express, spring, angular
-----------------
🟢 1. Library
📌 What it is
A library is a collection of pre-written code (functions/classes)
that you use to perform specific tasks.

📦 Examples in Node.js
- axios   → API calls
- lodash  → utility functions
- moment  → date handling

👉 Key idea:
You control when and where to use it in your code.


🔵 2. Framework
📌 What it is
A framework is a complete structure or skeleton for building applications.

👉 It provides:
- architecture
- rules
- flow control

👉 Extra points:
- You write code inside the framework structure
- Framework controls the execution flow (Inversion of Control)
- It gives built-in features like routing, middleware, lifecycle handling
- Helps in building large and structured applications
- Example: Express.js
===========================================================
assignment 
npm link , create two folders/packages and try it out