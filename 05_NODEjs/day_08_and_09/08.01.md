just introduction
==================================

// index.js

// import mysql package
const mysql = require("mysql2");
const { faker } = require('@faker-js/faker');
const { v4: uuidv4 } = require('uuid');

// create the connection to database
const connection = mysql.createConnection({
    host: 'localhost',
    user: 'root',
    database: 'demo_app',
    password: 'nithin@061'
});

// execute query
connection.query(
    "SELECT * FROM user",
    function (err, result, fields) {
        if (err) {
            console.log(err);
            return;
        }

        console.log(result);
    }
);

// close connection (optional but good practice)
connection.end();

let q = `INSERT INTO user ()"

let q = `INSERT INTO user (id, username, email, password) VALUES ?';
let user_details = [uuidv4(), 'nithin', 'nithin@example.com', 'password123'];

let data = [];
for (let i = 0; i < 100; i++) {
data.push([
uuidv4(),
faker.internet.userName(),
faker.internet.email(),
faker. internet.password()

]);


connection.query(q,[data],

function (err, rēsult) {
if (err) {
console.log(err);
return;
}
console.log(result);
}
);
now run this and it will create entry in database and u can see in command prompt below using select * .....

====
now we install faker, for dummy entries in database for practice or exlporation

=========
command prompt mysql - user
show databases;
use demo_app;
show tables;
DESCRIBE user; // for table structure;

CREATE TABLE user (
id VARCHAR(59) PRIMARY KEY,
username VARCHAR(50) UNIQUE,
email VARCHAR(50) UNIQUE NOT NULL,
password VARCHAR(50) NOT NULL
now drop table and make freshly again
npm install uuid;

================='
// A simple SELECT query
connection.query(
'SELECT * FROM 'table' WHERE "name' = "Page" AND 'age' > 45',
function (err, results, fields) {
console. log(results); // results contains rows roturned by server
console.log(fields); // flelds contains extra meta data about results, if avallable

// Using placeholders
connection.query(
'SELECT * FROM `table' WHERE `name' = ? AND `age' > ?'
['Page', 45],
function (err, results) {
console.log(results);

// Close the connection
connection.end();

);

//Inserting New Data
let q = "INSERT INTO user (id, username, email, password) VALUES (?, 7, 7, ?)";
Let usert= ["123b", "123_newuserb", "abc@gmail.comb", "abcb"];

try {
connection.query(q, user, (err, result) =>
if (err) throw err;
console.log(result);

} catch (err) {
console. log(err);

===========
Do Activity

Routing
REST

GET/ show number of users in db

GET/user show users(email, id username)

PATCH/ username edit

