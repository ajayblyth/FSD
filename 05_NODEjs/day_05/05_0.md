
HTTP (HYPERTEXT TRANSFER PROTOCOL)
=============================================================================

HTTP is the communication protocol used between client and server: 

follows REQUEST - RESPONSE MODEL

Browser sends REQUEST  →  Server sends RESPONSE

HTTP
-----------------------------------------------------------------------------
• Carries/transfers the data

API
-----------------------------------------------------------------------------
• Defines endpoint/service for data exchange

-----------------------------------------------------------------------------
EXAMPLE
-----------------------------------------------------------------------------

You open:  https://google.com

1. Browser sends HTTP request
2. Google server processes request
3. Server sends HTTP response
4. Browser displays webpage

=============================================================================
HTTP IS STATELESS
=============================================================================

• Server does NOT remember previous requests automatically
• Every request is independent

-----------------------------------------------------------------------------
EXAMPLE
-----------------------------------------------------------------------------
Request 1 → login     Request 2 → open profile

Server does not remember Request 1 automatically.

-----------------------------------------------------------------------------
SO HOW LOGIN WORKS?
-----------------------------------------------------------------------------
Using:  • Cookies    • Sessions    • Tokens (JWT)

=============================================================================
HTTP REQUEST
=============================================================================

Request = message sent by client to server.

-----------------------------------------------------------------------------
CONTAINS
-----------------------------------------------------------------------------
• Method    • URL    • Headers (Content-Type   )   • Optional Body

=============================================================================
HTTP REQUEST STRUCTURE
=============================================================================

GET /users HTTP/1.1
Host: example.com

-----------------------------------------------------------------------------
PARTS
-----------------------------------------------------------------------------
GET → method     /users → route/resource
HTTP/1.1 → protocol version     Host → header

=============================================================================
HTTP METHODS
=============================================================================

GET     → Fetch/read data
POST    → Send/create data
PUT     → Update entire data
PATCH   → Partial update
DELETE  → Delete data


GET vs POST (INTERVIEW POINT)
-----------------------------------------------------------------------------
GET
• Used to retrieve data from server.
• Data is sent in URL (query parameters).
• Visible in browser URL.
• No request body is used.

POST
• Used to send/create data on server.
• Data is sent in request body.
• Not visible in URL.
• Supports large amount of data.

Handling Post requests
Set up POST request route to get some response

Parse POST request data

JavaScript
app.use(express.urlencoded({ extended: true }));
app.use(express.json());
=============================================================================
HTTP RESPONSE
=============================================================================

Response = message sent by server back to client.

-----------------------------------------------------------------------------
CONTAINS
-----------------------------------------------------------------------------
• Status code    • Headers    • Optional Body/Data

=============================================================================
HTTP RESPONSE STRUCTURE
=============================================================================

HTTP/1.1 200 OK
Content-Type: application/json

{
  "name": "Ajay"
}

-----------------------------------------------------------------------------
PARTS
-----------------------------------------------------------------------------
HTTP/1.1 → protocol version     200 OK → status code
Content-Type → header           JSON → response body

HTTP STATUS CODES
-----------------------------------------------------------------------------
CODE   MEANING
-----------------------------------------------------------------------------
1xx    Informational
2xx    Success
       200 OK
       201 Created

3xx    Redirection
       301 Moved Permanently
       302 Found

4xx    Client Error
       400 Bad Request
       401 Unauthorized

       403 Forbidden
       404 Not Found

5xx    Server Error
       500 Internal Server Error
       503 Service Unavailable


=============================================================================
HEADERS
=============================================================================

Headers = extra metadata information.

EXAMPLES

Content-Type: application/json
Authorization: Bearer token
Content-Length: 120

=============================================================================
REQUEST BODY
=============================================================================

Body contains actual data sent to server.

-----------------------------------------------------------------------------
EXAMPLE
-----------------------------------------------------------------------------

POST /users

{
  "name": "Ajay",
  "age": 22
}

=============================================================================
WHY HTTP IS IMPORTANT
=============================================================================

• Base of web development
• Used in frontend + backend
• APIs work using HTTP
• Browser-server communication depends on HTTP

=============================================================================
HTTP vs HTTPS
=============================================================================

HTTP
-----------------------------------------------------------------------------
• Data sent in plain text
• Less secure

HTTPS
-----------------------------------------------------------------------------
• Encrypted using SSL/TLS
• Secure communication
• Used in modern websites

=============================================================================
COMMON FLOW IN WEB DEVELOPMENT
=============================================================================

Frontend sends HTTP request
            ↓
Backend/API receives request
            ↓
Database operation happens
            ↓
Backend sends HTTP response
            ↓
Frontend displays result

=============================================================================
EXPRESS.JS EXAMPLE
=============================================================================

app.get("/users", (req, res) => {

  res.status(200).json({
    message: "Users fetched"
  });
});

-----------------------------------------------------------------------------
FLOW
-----------------------------------------------------------------------------
Client Request  →  Route Handler  →  Response

=============================================================================
IMPORTANT INTERVIEW UNDERSTANDING
=============================================================================

“HTTP is a stateless request-response protocol used for
communication between clients and servers on the web.”

-------------------------------------------------------------------
QUERY PARAMETERS
-----------------------------------------------------------------------------
• Extra data in URL after '?'

Example:
-----------------------------------------------------------------------------
/api/users?age=25&city=Delhi

• Used for filtering/searching

-----------------------------------------------------------------------------

PATH PARAMETERS
-----------------------------------------------------------------------------
• Dynamic values in URL path

Example:
-----------------------------------------------------------------------------
/api/users/101

• Here 101 is user ID

-----------------------------------------------------------------------------

IDEMPOTENT METHODS (IMPORTANT CONCEPT)
-----------------------------------------------------------------------------
Making the same request multiple times produces the same result.

Idempotent:
GET, PUT, DELETE

Not Idempotent:
POST

Result:
Ajay1, Ajay2, Ajay3 ❌
- even if u send same data, new id is generated for each
👉 State changes every time → NOT idempotent ❌
-----------------------------------------------------------------------------

COMMON FLOW (REAL WORLD)
-----------------------------------------------------------------------------
1. Client sends request (GET /users)
2. Server processes request
3. Server sends response (data + status code)

-----------------------------------------------------------------------------

TOOLS TO TEST HTTP REQUESTS
-----------------------------------------------------------------------------
• Browser (for GET)
• Postman
• cURL
• Axios / Fetch (JavaScript)

=============================================================================
FETCH API vs AXIOS
=============================================================================

Both Fetch and Axios are used to:
• Send HTTP requests
• Communicate with APIs/servers
• Receive response data

=============================================================================
FETCH API
=============================================================================

• Built-in browser API
• No installation needed
• Promise-based
• Used for making HTTP requests

-----------------------------------------------------------------------------
FETCH EXAMPLE (JS)
-----------------------------------------------------------------------------

fetch('/api/users')
  .then(res => res.json())
  .then(data => console.log(data));

-----------------------------------------------------------------------------
IMPORTANT
-----------------------------------------------------------------------------
• fetch() returns Response object
• res.json() converts response → JavaScript object
• Does not reject promise for HTTP errors automatically

=============================================================================
AXIOS
=============================================================================

• Popular external HTTP library
• Must install separately
• Simpler syntax than fetch
• Automatically converts JSON response

-----------------------------------------------------------------------------
INSTALL
-----------------------------------------------------------------------------

npm install axios

-----------------------------------------------------------------------------
AXIOS EXAMPLE (JS)
-----------------------------------------------------------------------------

axios.get('/api/users')
     .then(res => console.log(res.data));

-----------------------------------------------------------------------------
IMPORTANT
-----------------------------------------------------------------------------
• Response data directly available in res.data
• Better error handling
• Supports interceptors, timeout, request cancellation

=============================================================================
KEY DIFFERENCE
=============================================================================

FETCH
-----------------------------------------------------------------------------
• Built into browser
• Manual JSON conversion
• More low-level

AXIOS
-----------------------------------------------------------------------------
• External library
• Automatic JSON parsing
• Cleaner + feature-rich

=============================================================================
QUICK UNDERSTANDING
=============================================================================

Frontend apps use:
• fetch()
• axios

to call backend APIs using HTTP requests.

=============================================================================

IMPORTANT INTERVIEW POINTS
-----------------------------------------------------------------------------
• Difference: PUT vs PATCH
• Difference: GET vs POST


=============================================================================
API (APPLICATION PROGRAMMING INTERFACE)
=============================================================================

• API is a bridge/interface that allows two software systems to communicate.
• In web development, APIs allow frontend and backend to exchange data.
• Client sends HTTP request → API processes it → returns response.
• APIs usually send data in JSON format.
• Frontend does not access database directly; it talks through API.
• API hides internal backend/database logic from users.
• Common HTTP methods used in APIs:
  GET, POST, PUT, DELETE.
• Example:
  Weather app gets weather data from weather API.
• Backend frameworks like Express/Spring Boot create APIs.
• Modern web apps, mobile apps, payment systems, and cloud services heavily depend on APIs.

=============================================================================
• What is stateless protocol?
A stateless protocol is one where the server does NOT remember
previous requests. Each request is treated as a completely new request.

In HTTP:
- No memory of past interaction
- Every request must contain all required information

• What are status codes? they indicate the  result of a request.
• What are headers?
• What is REST API?

REST (Representational State Transfer) is an architectural style  to design APIs
using standard HTTP methods.

Key features:
- Stateless communication
- Uses HTTP methods (GET, POST, PUT, PATCH, DELETE)
- Resource-based URLs (/users, /products)
- Usually returns JSON

Example:
GET /users      → fetch users
POST /users     → create user
PATCH /users/1  → update user
DELETE /users/1 → delete user

STATELESS vs STATEFUL + SESSION (SHORT)
===================================================================

Stateless → Server does NOT store client data between requests
Each request must contain all required information

How it works:
- Every request is independent
- Server does not remember previous requests or login state

Stateful → Server stores user data (state) using sessions
- Remembers user across requests

Session:
- Server-side storage to keep user data (like login info)
- Server creates session → sends session ID (cookie)
- Client sends session ID in every request → server identifies user

Example:
Login → session created → user stays logged in (stateful)

Stateless (REST):
- No session memory
- Client sends token (JWT) every time

One-line:
Stateless = no memory, Stateful = server remembers using sessions

=============================================================================
URL ENCODED IN SERVER (EXPRESS JS)
=============================================================================

WHY NEEDED
-----------------------------------------------------------------------------
• Browser sends form data in urlencoded format.
• Server cannot read it directly.
• Middleware is required to parse it into usable object.

-----------------------------------------------------------------------------

EXPRESS MIDDLEWARE
-----------------------------------------------------------------------------
app.use(express.urlencoded({ extended: true }));

-----------------------------------------------------------------------------

WHAT IT DOES
-----------------------------------------------------------------------------
• Converts incoming data into JavaScript object.
• Makes data available inside: req.body

Example Input (from form):
-----------------------------------------------------------------------------
name=Ajay&age=25

After parsing:
-----------------------------------------------------------------------------
req.body = {
  name: "Ajay",
  age: "25"
}

-----------------------------------------------------------------------------

EXTENDED: TRUE vs FALSE
-----------------------------------------------------------------------------
extended: true
• Uses "qs" library
• Supports nested objects

Example:
user[name]=Ajay&user[age]=25

→ req.body:
{
  user: {
    name: "Ajay",
    age: "25"
  }
}

-----------------------------------------------------------------------------

extended: false
• Uses "querystring" (simple parsing)
• Does NOT support nested objects

Same input:
user[name]=Ajay

→ req.body:
{
  "user[name]": "Ajay"
}

-----------------------------------------------------------------------------

FULL EXPRESS EXAMPLE
-----------------------------------------------------------------------------
const express = require('express');
const app = express();

app.use(express.urlencoded({ extended: true }));

app.post('/submit', (req, res) => {
    console.log(req.body);
    res.send("Data received");
});

app.listen(3000); //app.listen() starts the server and listens for incoming requests on a port

-----------------------------------------------------------------------------

HTML FORM EXAMPLE
-----------------------------------------------------------------------------
<form action="/submit" method="POST">
    <input type="text" name="name">
    <input type="number" name="age">
    <button type="submit">Send</button>
</form>

-----------------------------------------------------------------------------

IMPORTANT POINTS
-----------------------------------------------------------------------------
• Works only for urlencoded data (forms)
• For JSON → use express.json()
• Must be used BEFORE routes
• req.body is undefined without middleware

-----------------------------------------------------------------------------

FLOW (END TO END)
-----------------------------------------------------------------------------
1. User submits form
2. Browser sends urlencoded data
3. express.urlencoded() parses it
4. Data available in req.body
5. Server processes it

=============================================================================
