=====================================================================
CALLBACK (JavaScript)
=====================================================================

Definition
A callback is a function passed as an argument to another function
and executed later after a task is completed.


---------------------------------------------------------------------
Example
---------------------------------------------------------------------

function greet(name, callback) {
    console.log("Hello " + name);
    callback();                     // execute callback
}

function sayBye() {
    console.log("Goodbye!");
}

greet("Ajay", sayBye);


Output
Hello Ajay
Goodbye!


Explanation
• sayBye is passed to greet() as a function argument
• greet() executes the callback after printing the greeting



=====================================================================
CALLBACK HELL
=====================================================================

Definition
Callback hell occurs when multiple callbacks are nested inside
each other, making the code hard to read, debug, and maintain.


---------------------------------------------------------------------
Example
---------------------------------------------------------------------

setTimeout(() => {

    console.log("Step 1");

    setTimeout(() => {

        console.log("Step 2");

        setTimeout(() => {

            console.log("Step 3");

            setTimeout(() => {

                console.log("Step 4");

            }, 1000);

        }, 1000);

    }, 1000);

}, 1000);



Structure

callback
   └─ callback
        └─ callback
             └─ callback



Problems
• Code becomes deeply nested
• Difficult to read
• Difficult to debug
• Difficult to maintain


Modern Solutions
• Promises
• Async / Await



---------------------------------------------------------------------
Interview One-Line Answers
---------------------------------------------------------------------

Callback
A function passed as an argument to another function and executed
later after the task completes.

Callback Hell
A situation where nested callbacks make the code difficult to
read and maintain.
=====================================================================
Promises
A Promise is an object representing the eventual completion or failure of an asynchronous operation. It serves as a cleaner, more robust alternative to traditional callbacks, all for better handling of asynchronous flows.

The Three States of a Promise
Pending: The initial state; the asynchronous operation has not yet completed or failed.

Fullfill: The operation completed successfully, and the promise now holds a resulting value.

Reject: The operation failed, and the promise holds the reason for the failure (an error).

Code Example
JavaScript
function savetoDb(data) {
    return new Promise((success, failure) => {
        let internetSpeed = Math.floor(Math.random() * 10) + 1;
        if (internetSpeed > 4) {
            success();
        } else {
            failure();
        }
    });
}
Usage Snippet
JavaScript
let request = saveToDbPromise("OpqTech");

request
    .then(() => {
console.log("promise resolved") ;   })
.catch(()=>{
    console.log("promise rejected");
});