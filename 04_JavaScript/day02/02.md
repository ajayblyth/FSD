============================================================================================================================
JAVASCRIPT IDENTIFIERS
============================================================================================================================

Definition:
Identifiers are names used for variables, functions or objects in JavaScript.

----------------------------------------------------------------------------------------------------------------------------
RULES
----------------------------------------------------------------------------------------------------------------------------
1) Allowed characters → letters (a-z, A-Z), digits (0-9), underscore (_), dollar ($)
2) Must start with letter, _ or $
3) Cannot start with a digit
4) Case-sensitive (myVar and MyVar are different)
5) Cannot use reserved keywords
6) No spaces or special characters except _ and $
7) Should be meaningful (best practice)

----------------------------------------------------------------------------------------------------------------------------
EXAMPLES
----------------------------------------------------------------------------------------------------------------------------
Valid

var myVar;
let _count;
const $price = 100;
function getData(){}

Invalid

var 1stValue;      // starts with digit
let my-var;        // contains hyphen
const var = 10;    // reserved keyword

----------------------------------------------------------------------------------------------------------------------------
COMMON NAMING STYLES
----------------------------------------------------------------------------------------------------------------------------
camelCase   → myVariable, totalAmount, getUserData
PascalCase  → UserProfile, EmployeeDetails
snake_case  → my_variable, total_amount


============================================================================================================================
BOOLEAN IN JAVASCRIPT
============================================================================================================================

Definition:
Boolean is a logical data type with only two values → true or false.

Used in conditions, loops, and comparisons.

Examples

let isActive = true;
let hasAccess = false;

Returned by comparisons

5 > 3        // true
10 === "10"  // false

Key Points
1) Only two values → true / false
2) Case-sensitive
3) Used in logical expressions and conditions


============================================================================================================================
WHAT IS TYPESCRIPT
============================================================================================================================

Definition:
TypeScript is a superset of JavaScript developed by Microsoft that adds static typing.
It compiles to plain JavaScript.
----------------------------------------------------------------------------------------------------------------------------
KEY FEATURES
----------------------------------------------------------------------------------------------------------------------------
• Static type checking
• Better IDE support (autocomplete, refactoring)
• Supports modern ECMAScript features
• OOP support (classes, interfaces, modules)
• Gradual adoption in existing JS projects

Example

JavaScript
let age = 23;
age = "twenty three";     // allowed

TypeScript
let age: number = 23;
age = "twenty three";     // error


============================================================================================================================
STRINGS IN JAVASCRIPT
============================================================================================================================
A string is a primitive data type used to store text (a sequence of characters).
It can be created using single quotes ('), double quotes ("), or template literals (`).
Strings are immutable, meaning they cannot be changed after creation.
JavaScript provides many built-in methods like length, charAt(), slice(), substring(), replace(), and toUpperCase().
Strings are compared lexicographically based on their Unicode values.

----------------------------------------------------------------------------------------------------------------------------
DECLARATION
----------------------------------------------------------------------------------------------------------------------------

let name = "Ajay";
let city = 'Delhi';
let greeting = `Hello ${name}`;

----------------------------------------------------------------------------------------------------------------------------
KEY FEATURES
----------------------------------------------------------------------------------------------------------------------------

1) Immutable (cannot modify characters)
2) Zero-based indexing
3) Supports many built-in methods

-Indexing means accessing elements or characters using their position number,
works only for reading string and not changing it.

Index access
let str = "JavaScript";
str[0]    // "J"
str[4]    // "S"

Length

str.length


============================================================================================================================
COMMON STRING METHODS
============================================================================================================================

let str = "  Hello World  ";

str.toUpperCase()      // "  HELLO WORLD  "
str.toLowerCase()      // "  hello world  "

str.trim()             // "Hello World"

slice(start, end)
start → Inclusive ✅
end   → Exclusive ❌

str.slice(2,7)         // "Hello"   

str.substring(2,7)     // "Hello", same as slice start/end

Note:
Both slice() and substring() extract part of a string using an inclusive start index and an exclusive end index. However, slice() supports negative indices and does not swap arguments, whereas substring() treats negative values as 0 and swaps the arguments if the start index is greater than the end index.

let str = "JavaScript";
str.slice(-6); // "Script" ✅

string.replace(searchValue, replaceValue)
str.replace("World","JS")   // "  Hello JS  "

str.includes("Hello")  // true


split() divides a string into an array of substrings based on a specified separator and returns the resulting array.
string.split(separator, limit)  
limit (Optional) → Maximum number of elements to return.

str.split(" ")         // ["", "", "Hello", "World", "", ""]

//Why? Because every space is treated as a separator, including the leading and trailing spaces, producing empty strings.

string.concat(string1, string2, ..., stringN)
"Hello".concat(" ","JS")  // "Hello JS"

str.charAt(2)          // "H"
string.indexOf(searchValue, startIndex)
startIndex (Optional) → Index from which to start searching.
str.indexOf("World")   // 8

string.lastIndexOf(searchValue, startIndex)
str.lastIndexOf("l")   // 11


Interview One-Liners
indexOf() → Returns the index of the first occurrence of a specified value, or -1 if not found.
lastIndexOf() → Returns the index of the last occurrence of a specified value, or -1 if not found.
============================================================================================================================
slice() vs substring()
============================================================================================================================

Both extract part of a string.

let str = "HelloWorld";

slice()

str.slice(-5)        // "World"

substring()

str.substring(-5)    // "HelloWorld"   (negative treated as 0)


============================================================================================================================
TEMPLATE LITERALS
============================================================================================================================

Definition:
Template literals use backticks (`) and allow variable interpolation.

Interpolation means inserting variables or expressions inside strings.

Example

let name = "Ajay";

`Hello ${name}`

Uses
• Dynamic strings
• String formatting
• Logging and messages


============================================================================================================================
STRING IMMUTABILITY
============================================================================================================================

Definition:
Strings are immutable → characters cannot be changed after creation.

Example

let str = "hello";

str[0] = "H";

console.log(str);

Output
hello

Reason:
JavaScript does not modify the original string.  
Instead it creates a new string.

Example

let str = "hello";

str = str + " world";  // ✅ Reassignment

console.log(str);

Output
hello world

const:
const str = "hello";

str = str + " world";   // ❌ TypeError


============================================================================================================================
STRING METHODS AND IMMUTABILITY
============================================================================================================================

Methods return a NEW string and do not modify the original.

Example

let str = "hello";

let result = str.toUpperCase();

console.log(str);      // hello
console.log(result);   // HELLO


============================================================================================================================
STRING vs ARRAY
============================================================================================================================

Strings → Immutable  
Arrays  → Mutable

Example

let arr = [1,2,3];
arr[0] = 10;

console.log(arr);   // [10,2,3]


let str = "abc";
str[0] = "z";

console.log(str);   // "abc"


============================================================================================================================
STRING CONCATENATION
============================================================================================================================

Definition:
Combining two or more strings.

Methods

1) + operator

firstName + " " + lastName

2) += operator

msg += " World"

3) concat()

"Hello".concat(" ","World")

4) Template literals(recommended)

`Hello ${name}`


============================================================================================================================
NULL AND UNDEFINED
============================================================================================================================

undefined
• Variable declared but not assigned value

let x;
console.log(x);   // undefined

null
• Intentional absence of value

let y = null;

Key Points

typeof undefined   // "undefined"
typeof null        // "object"

undefined → not assigned  
null → intentionally empty value

Note:
typeof null returning "object" is a historical JavaScript bug.


============================================================================================================================
QUOTES INSIDE STRINGS
============================================================================================================================

Single quotes

let msg = 'He said "Hello"';

Double quotes

let text = "It's a nice day";


============================================================================================================================
console.log()
============================================================================================================================

Definition:
Used to print messages to the console.

Purpose
• Debugging
• Checking values
• Testing logic

Syntax

console.log(value);

Examples

console.log("Hello World");
console.log(age);
console.log("Sum =", a + b);

Key Points
• Output appears in browser console
• Can print multiple values
• Does not affect webpage UI


============================================================================================================================
TRUTHY AND FALSY VALUES
============================================================================================================================

Every value in JavaScript behaves as either truthy or falsy in boolean context.

Example context
if, &&, ||, !

----------------------------------------------------------------------------------------------------------------------------
FALSY VALUES (ONLY 7)
----------------------------------------------------------------------------------------------------------------------------

false
0
-0
0n
""
null
undefined
NaN

Example

if (0) {
    console.log("Runs");
} else {
    console.log("Falsy");
}

----------------------------------------------------------------------------------------------------------------------------
TRUTHY VALUES
----------------------------------------------------------------------------------------------------------------------------

All other values are truthy.

true
1
-5
"Hello"
"0"
[]
{}
function(){}

Example

if ("Hello") {
    console.log("Truthy");
}

----------------------------------------------------------------------------------------------------------------------------
CONFUSING CASES
----------------------------------------------------------------------------------------------------------------------------

Boolean("0")   // true
Boolean([])    // true
Boolean({})    // true

Rule:
Only the 7 listed values are falsy.  
Everything else is truthy.