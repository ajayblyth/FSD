Q/A sessions


asked questions:
whats html
head vs body
html structure
table structure
can we put block level element inside inline
<!doctype html>


Selectors-class , id ,element , universal , group using commas, descendant using space like div p a{}, sibling +

------------------
pseudo class- tells pecial state of element, hover, focus(input active), checked (checkbox selected), 
example: button:hover{color:green}.


pseudo element:
Symbol: ::
• Styles a PART of an element

Example:
p::first-letter { font-size: 2em; }


SELECTOR PRIORITY (SPECIFICITY)
--------------------------------------------------------
Inline  >  ID  >  Class  >  Element
 !important
--------------------------------------------------------
Rule:
• Overrides everything

===========================


FORMS & INPUTS
------------------------------------------------------------
33. What is the purpose of an HTML form?
34. Why do input elements need the name attribute?
35. What is the difference between placeholder and value?
36. Why do radio buttons use the same name?
37. What is the difference between GET and POST?
38. Why is <label> important for inputs?


LISTS & TABLES
------------------------------------------------------------
39. What is the difference between ordered and unordered lists?
40. When should we use tables and when should we avoid them?
41. What is the role of <thead>, <tbody>, and <tfoot>?


HTML + CSS RELATIONSHIP
------------------------------------------------------------
42. How does HTML work together with CSS?
43. Why should we not use HTML for styling?
44. What happens if CSS is removed from a page?


COMMON CONFUSIONS / EDGE CASES
------------------------------------------------------------
45. Why does HTML ignore extra spaces and new lines?
46. Why does text sometimes appear on the same line even when we write it on a new line?
47. What happens if we forget to close a tag?
48. Is HTML case-sensitive? Why?
49. Why is nesting of elements important?


FINAL CONCEPTUAL
------------------------------------------------------------
50. How does the browser read and render an HTML file?